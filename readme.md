If you ecs instance is in the beijing region, you can use the following registry address:

```bash
registry-vpc.cn-beijing.aliyuncs.com
```

to replace the default registry address:

```bash
registry.cn-beijing.aliyuncs.com
```
